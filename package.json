{
  "name": "haip",
  "version": "0.0.1",
  "description": "HAIP (Hotel AI Platform) — Open-source, API-first hotel PMS",
  "private": true,
  "license": "MIT",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "build": "pnpm -r run build",
    "dev": "pnpm --filter @haip/api run dev",
    "lint": "pnpm -r run lint",
    "typecheck": "pnpm -r run typecheck",
    "test": "pnpm -r run test",
    "test:coverage": "pnpm -r run test:coverage",
    "db:generate": "pnpm --filter @haip/database run db:generate",
    "db:migrate": "pnpm --filter @haip/database run db:migrate",
    "db:studio": "pnpm --filter @haip/database run db:studio",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down",
    "clean": "pnpm -r run clean && rm -rf node_modules"
  }
}
